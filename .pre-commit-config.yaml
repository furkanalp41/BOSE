# Pre-commit secret scanning. Install once:
#   pipx install pre-commit && pre-commit install
# Then every `git commit` is scanned; a leaked secret blocks the commit.
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.18.4
    hooks:
      - id: gitleaks
